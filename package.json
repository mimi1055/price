{
  "name": "tavern-ledger",
  "version": "0.4.3",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.mjs", "check": "node --check index.js && node --check lib/st-storage.mjs" },
  "engines": { "node": ">=20" }
}
